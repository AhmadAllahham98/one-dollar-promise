import React from "react";
import { MainTemplate } from "../templates/MainTemplate";

export const LoadingPage = () => {
  return (
    <MainTemplate alignment="center">
      {/* Empty page content while loading */}
    </MainTemplate>
  );
};
